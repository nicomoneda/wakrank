<%= render TurboModalComponent.new(title: "Modifier personnage") do %>
    <%= simple_form_for [@character] do |f| %>
        <%= f.input :name, label: "Nom" %>
        <%= f.input :level, label: "Niveau" %>
        <%= f.submit "Créer" %>
    <% end %>  
<% end %>