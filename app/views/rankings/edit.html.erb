<%= render TurboModalComponent.new(title: "Nouveau classement") do %>
    <%= simple_form_for [@ranking] do |f| %>
        <%= f.input :stasis %>
        <%= f.button :submit, "Mettre à jour" %>
    <% end %>
<% end %>