<%= turbo_frame_tag "modal" do %>
    <%= tag.div data: { controller: "turbo-modal" }, style: "padding: 30px;" do %>
        <%= simple_form_for [@ranking] do |f| %>
            <%= f.input :stasis %>
            <%= f.button :submit, "Mettre à jour" %>
        <% end %>
        <%= button_tag "Annuler", data: { action: "turbo-modal#hideModal" }, type: "button", class: "rounded-lg py-3 px-5 bg-red-600 text-white" %>
    <% end %>
<% end %>